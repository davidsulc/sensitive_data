defmodule SensitiveData.Redacted do
  @moduledoc """
  An empty module serving only to indicate that data has been redacted.

  This typically shows up in redacted stack traces, as well as in cases
  where custom redaction functions fail.
  """
end
